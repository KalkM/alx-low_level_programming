#include "main.h"
/**
 *print_line- prints _ n times
 *@n: number of _
 * Return: something
 */
void print_line(int n)
{
int i;
if (n >= 0)
_putchar('\n');
for (i = 0; i < n; i++)
_putchar('_');
return (0);
}
